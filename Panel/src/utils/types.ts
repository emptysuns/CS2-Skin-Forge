export interface Loadout {
  weaponPaints: Record<number, number>;
  knifeIndex: number;
  knifePaint: number;
  gloveIndex: number;
  glovePaint: number;
  agentModel: number;
  musicKit: number;
  useRandom: boolean;
}
